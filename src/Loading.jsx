import React from 'react';
import blocks from './Blocks.svg'

const Loading = () => {
    return (
        <img alt='loading' src={blocks}>
            
        </img>
    );
};

export default Loading;